# Module `(nexus_interface=0x0)::v1`

The legacy witness-based interface announcement path has been retired.
Current Move code keeps only the shared-object reference/config helper types
needed by verifier compatibility and historical decoding.

New TAP execution must use `nexus_interface::tap`.
